<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.deangelorieke.dnd">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".OpeningMenu"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DriveLogin"
            android:label="@string/title_activity_drive_login" />
        <activity android:name=".NewCharacter" />
        <activity android:name=".ExistingCharacters" />
        <activity android:name=".CharacterCreation" />
        <activity android:name=".InfoDatabase" />
        <activity android:name=".InfoMonsters" />
        <activity android:name=".InfoClasses" />
        <activity android:name=".InfoRaces" />
        <activity android:name=".DMMode" />
        <activity android:name=".statallocation" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".CharacterViewer"></activity>
    </application>

</manifest>